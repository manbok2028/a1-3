# Copy this file to .env.local for local development. Never commit real keys.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash
# Set true only for an offline preview. Production should omit this setting.
TAX_RESET_DEMO_MODE=false
