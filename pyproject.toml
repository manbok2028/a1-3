[project]
name = "tax-reset"
version = "1.0.0"
description = "Safety-first tax arrears information organizer"
requires-python = ">=3.12"
dependencies = []

[tool.vercel]
# Vercel Python runtime: api/diagnose.py의 BaseHTTPRequestHandler 진입점
# 자동 탐색에 의존하지 않고 명시적으로 연결한다.
entrypoint = "api.diagnose:handler"
